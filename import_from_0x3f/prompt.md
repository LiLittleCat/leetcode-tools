{
“role”: “user”,
“content”: “你是一个熟悉 LeetCode 题目体系的助手，任务是：根据键名信息，补全一个 JSON 对象中所有题单的 value。\n\n【输入格式】\n 调用时会传入一个 JSON 对象 {{TASK_MAP}}，形如：\n{\n "1 滑动窗口与双指针 / 一、定长滑动窗口 / §1.1 基础": "",\n "1 滑动窗口与双指针 / 一、定长滑动窗口 / §1.2 进阶（选做）": "",\n …\n}\n- key：为完整路径字符串 = 专题名称 + 分类 + 小节名称，用 "/" 分隔；\n- value：初始为空字符串 ""，需要由你填充。\n\n【你的任务】\n 根据每个 key 中的文本信息（包括专题名、章节名、小节名等），为对应的 value 生成一个「带编号的题单名称」。\n\n【输出要求】\n1. 不得增加、删除或修改任何 key，只更新各 key 对应的 value。\n2. 每个 value 必须是一个字符串，格式统一为：\n " "\n 例如："1.1 定长滑动窗口基础"。\n3. 关于 ID（全局序号）：\n - 使用多层级数字序号，如："1"、"1.1"、"2.3.1" 等；\n - ID 需体现全局的层级与顺序关系，保证：\n - 同一大类下的小节编号连续、清晰；\n - 整个专题范围内 ID 全局唯一、递增有序；\n - 可以依据键名中的章节结构（如「一、」「§1.1」等）推导出合理的多级 ID。\n4. 关于 TITLE（题单描述）：\n - 用简洁流畅的中文概括该题单的核心内容；\n - 可参考原始 key 中的最后一段或关键信息进行提炼、归纳与润色；\n - 每个 TITLE 不超过 30 个中文字符；\n - 若专题名在上下文已很明显，可省略重复的冗长部分，只保留有助于理解的关键信息。\n5. 只输出最终的 JSON 对象：\n - 结构与 {{TASK_MAP}} 完全相同（同样的 key 集合）；\n - 所有 value 都已按上述规则填好；\n - 不输出任何额外说明文字、示例或 Markdown 代码块标记。”
}
